# KDL characterization fixtures

These fixtures exercise the canonical KDL v2 subset accepted by `settei-kdl`. They are
package data so file-loading tests do not depend on the developer's working directory.

`nested.kdl` is the successful nested-object fixture used to prove package-local file
loading and source-path provenance.
